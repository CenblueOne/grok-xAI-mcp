{
  "mcpServers": {
    "cloudflare": {
      "command": "npx",
      "args": ["-y", "cloudflare-mcp-server"],
      "env": {
        "CLOUDFLARE_API_TOKEN": "YOUR_CLOUDFLARE_API_TOKEN_HERE"
      },
      "description": "Cloudflare DNS, Workers, R2, Pages"
    }
  }
}
