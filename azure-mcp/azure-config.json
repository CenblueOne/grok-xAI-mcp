{
  "mcpServers": {
    "azure": {
      "command": "npx",
      "args": ["-y", "@azure/mcp-server-azure"],
      "description": "Azure services (Resource, SQL, Blob, Functions, CosmosDB, dll)"
    }
  }
}
